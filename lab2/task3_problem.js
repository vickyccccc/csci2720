{
    // A simple arrow function look like this:
    let hello = () => alert("Hello!");

    // Convert the following function check() into an arrow function of one line
    function check(year) {
        if (year > 3)
            return true;
        else
            return false;
    }
}